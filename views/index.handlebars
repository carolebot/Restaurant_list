
{{!-- restaurants:restaurants --}}
{{#if restaurants}}
<!-- restaurant list -->
<div class="container mt-5">
  <div class="row">
    <div class="col-md-10 col-12 mx-auto">

      <div class="row d-flex justify-content-between p-3">
      <form action="/search">
        <div class="input-group ">
          <div class="input-group-append">
            <button class="btn btn-major" type="submit" id="search-button"><i class="fas fa-search"></i></button>
          </div>
          <input type="text" name="keyword" class="form-control" placeholder="name or category" aria-label="Restaurant Name..."
            value="{{keyword}}" aria-describedby="search-button">    
        </div>
      </form>
      <!-- search bar -->
      <div class="Sort">
        <select class="custom-select" id="inputGroupSelect02">
          <option selected>Choose...</option>
          <option value="1">One</option>
          <option value="2">Two</option>
          <option value="3">Three</option>
        </select>
      </div>
      </div>
      <div class="card-columns">
        {{#each restaurants}}
        <a href="/restaurants/{{this._id}}" class="text-secondary">
          <div class="card mb-3">
            <img class="card-img-top" src="{{this.image}}" alt="{{this.name}}">
            <div class="card-body p-3">
              <h6 class="card-title mb-1">{{this.name}}</h6>

              <div class="restaurant-category mb-1">
                <i class="fas fa-utensils pr-2"></i> {{this.category}}
              </div>
              <span class="badge badge-pill badge-danger font-weight-normal">
                {{this.rating}}
                <i class="fas fa-star fa-xs"></i>
              </span>
            </div>
            <div class="card-footer bg-transparent d-flex align-items-center justify-content-end">
              <a href="./restaurants/{{this._id}}/edit">Edit</a>
              <a href="./restaurants/{{this._id}}" class="mx-2">Details</a>
              <form action="/restaurants/{{this._id}}/delete" method="POST" style="display: inline;">
                <button type="submit" class="btn btn-danger btn-sm">delete</button>
              </form>
            </div>
          </div>
        </a>
        {{/each}}
      </div>
    </div>
  </div>
</div>
{{/if}}

{{#unless restaurants}}
{{!-- 搜尋無結果提醒 --}}
<div class="container mt-5">
  <div class="row">
    <div class="col-md-10 col-12 mx-auto text-center">
      很抱歉，您搜尋的關鍵字沒有任何結果:( <br> 回<a href="/">我的餐廳清單</a> 可以試著搜尋看看其他關鍵字唷＾＾
    </div>
  </div>
</div>
{{/unless}}